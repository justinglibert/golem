master_node:
  # Private ip address of the node
  ip: '127.0.0.1'
  port: '121212'
nodes:
  local:
    # host can also be a ssh host name
    host: '127.0.0.1'
    user: 'justinglibert'
    python_binary: '/Users/justinglibert/.pyenv/shims/python'
    hardware:
      cpus: 8
